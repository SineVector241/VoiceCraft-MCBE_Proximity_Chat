## Tooling for certain external tools.

adbStarter.sh (starts the android debugger server BECAUSE LINUX ISSUES WITH RIDER)
adbStarter.sh (stops the android debugger server BECAUSE LINUX ISSUES WITH RIDER)
